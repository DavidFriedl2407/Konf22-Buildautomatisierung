package Abschreibung;

public class hallo {

    //HashMap<String, List<Integer>> gradesofStudents = new HashMap<>();

    //for (String student: gradesofStudents.keySet()){
    //List <Integer> grades= gradesofStudents.get(student);
    //sout (student+grades);

    //for (Person person:super.getPersons()){
    //String country = person.getCountry();
    //if (result.containsKey(Country)
    //reslult.put (country, result get (country)+1

    //else result put country, 1
}
